Stockfish for shuffleboard chess.

To run, clone the repo to your desired project folder. Then, from the root of the project, run

```pip install -r requirements.txt```

and run 'main.py' with python in your terminal.
